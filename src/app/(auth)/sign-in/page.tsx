import React from "react";

const page = () => {
    return (
        <div>
            Welcome. Please do this to sign in and continue using the
            application
        </div>
    );
};

export default page;
